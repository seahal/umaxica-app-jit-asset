{
  "name": "umaxica-app-jit-asset",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "lint": "bunx @biomejs/biome lint --write ./src",
    "format": "bunx @biomejs/biome format --write ./src",
    "check": "bunx @biomejs/biome check --write ./src",
    "test": "bun test"
  },
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "@types/bun": "latest",
    "biome": "latest",
    "wrangler": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "hono": "^4.7.5"
  }
}
